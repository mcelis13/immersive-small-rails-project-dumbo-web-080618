<!--
<ul>
  <% @user_products.each do |user_product| %>
    <% if user_product.user != @user %>
      <a href="<%= user_product_path(user_product) %>" class="product_img"><%= image_tag("#{user_product.product.img_url}", :width => "100") %></a>
      <li><%= link_to user_product.product.name, user_product_path(user_product) %></li>
    <% end %>
  <% end %>
</ul>
 -->

 <div class="iconbox">
    <% @user_products.each do |user_product| %>
      <% if user_product.user != @user %>
        <div class="aaa"><p><a href="<%= user_product_path(user_product) %>"><%= image_tag("#{user_product.product.img_url}", :width => "100") %></a><%= user_product.product.name %></p></div>
      <% end %>
    <% end %>
 </div>
