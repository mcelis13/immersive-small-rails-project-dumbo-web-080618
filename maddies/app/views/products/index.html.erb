<!-- <div class="products">
  <% @products.each do |product| %>
  <figure>
    <%= image_tag("#{product.img_url}", :width => "200") %>
    <figcaption><%= link_to product.name, product_path(product) %></figcaption>
  </figure>

  <% end %>
</div> -->

<div class="products">
  <% @products.each do |product| %>
    <a href="<%= product_path(product) %>" class="product_img"><%= image_tag("#{product.img_url}", :width => "200") %></a>
  <% end %>
</div>
